# Maxi Telephony Handler - Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================================================
# GOOGLE CALENDAR CONFIGURATION (Required)
# ============================================================================

# Base64-encoded Google Cloud Service Account JSON
# To generate: base64 -i service-account.json | tr -d '\n'
# See README for detailed setup instructions
GOOGLE_SERVICE_ACCOUNT_JSON_B64=your_base64_encoded_service_account_json_here

# Google Calendar ID (optional, defaults to "primary")
# Find in Google Calendar Settings → Integrate Calendar
# Example: your-calendar-id@group.calendar.google.com
GOOGLE_CALENDAR_ID=primary

# ============================================================================
# TWILIO SMS CONFIGURATION (Required for SMS notifications)
# ============================================================================

# Twilio Account SID
# Find at: https://console.twilio.com/
TWILIO_SID=your_twilio_account_sid_here

# Twilio Auth Token
# Find at: https://console.twilio.com/
TWILIO_AUTH=your_twilio_auth_token_here

# Twilio Phone Number (E.164 format: +1234567890)
# Purchase at: https://console.twilio.com/us1/develop/phone-numbers/manage/incoming
TWILIO_NUMBER=+1234567890

# ============================================================================
# EMAIL CONFIGURATION (Required for email notifications)
# ============================================================================

# Email address to send from
EMAIL_FROM=your-email@gmail.com

# Email password (for Gmail, use App Password, not regular password)
# Generate App Password: Google Account → Security → 2-Step Verification → App passwords
EMAIL_PASSWORD=your_app_password_here

# SMTP Server (default: smtp.gmail.com for Gmail)
SMTP_SERVER=smtp.gmail.com

# SMTP Port (default: 587 for TLS)
SMTP_PORT=587

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Environment identifier (for logging)
# Options: development, staging, production
ENVIRONMENT=production

# Port (automatically set by Railway, but can override for local testing)
PORT=8000
